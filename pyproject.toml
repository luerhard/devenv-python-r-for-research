[project]
description = "Add your description here"
name = "devenv-python-r-for-research"
readme = "README.md"
requires-python = "~=3.12"
version = "0.1.0"
dependencies = [
    "numpy>=2.2.6",
    "pandas>=2.2.3",
]

[build-system]
build-backend = "hatchling.build"
requires = ["hatchling"]

[tool.hatch.build]
include = ["src"]

[dependency-groups]
dev = [
  "jupyter>=1.1.1",
]
